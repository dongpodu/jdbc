目的：用来验证preparedStatement和statement
结果：preparedStatement在执行两次时，第一次传sql加上参数，第二次只传参数，而statement在执行两次时会传两次完整的sql
引申：sql每次执行前都要先编译，如果两个sql结构完全一样，只是参数不一要，则数据库第一次执行时编译一下sql，并缓存编译的结果，第二次执行时则无需再编译，直接用缓存中编译好的sql